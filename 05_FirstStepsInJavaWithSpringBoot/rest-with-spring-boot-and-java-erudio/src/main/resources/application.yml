spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_erudio?useTimezone=true&serverTimeZone=UTC
    username: root
    password: satodavi0
  jpa:
     open-in-view: false
     hibernate:
        ddl-auto: update
    # properties:
     #   hibernate:
      #   daliect: org.hibernate.dialect.MySQLDialect
     show-sql: false